#!/bin/sh
while read local_ref local_sha remote_ref remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "Direct pushes to main are disabled. Push a numbered version branch and open a pull request." >&2
    exit 1
  fi
done
node scripts/version-control.mjs check --commit
